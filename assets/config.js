// modify this object for git-proof frontend config as needed
// uncomment commented lines as needed

let conf = {
  //'url': '' // custom url override
  //'branding': '' // custom branding
  //'clusters': 5 // shards per cluster
};

//window.config = conf;
